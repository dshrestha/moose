import { toUpper } from 'moose/helpers/to-upper';
import { module, test } from 'qunit';

module('Unit | Helper | to upper');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = toUpper([42]);
  assert.ok(result);
});
