import { doAnd } from 'moose/helpers/do-and';
import { module, test } from 'qunit';

module('Unit | Helper | do and');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = doAnd([42]);
  assert.ok(result);
});
